type Props = {};

const Footer = (props: Props) => {
	return (
		<footer className="w-full p-4 bg-primary-400 md:p-8 lg:p-10">
			<div className="mx-auto max-w-screen-xl text-center"></div>
		</footer>
	);
};

export default Footer;
